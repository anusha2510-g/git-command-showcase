# Git Command Showcase

A quick documentation cheat sheet for core Git commands used in version control and collaboration.

## Clone Repository
```bash
git clone <repository-url>
git status
git add .
git commit -m "message"
git push
git checkout -b <branch-name>
git stash
